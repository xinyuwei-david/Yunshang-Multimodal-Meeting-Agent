[build-system]
requires = ["setuptools==75.2.0", "wheel==0.45.1"]
build-backend = "setuptools.build_meta"

[project]
name = "yunshang-meeting-agent"
version = "0.1.0"
description = "A provider-neutral multimodal meeting pipeline with a human-controlled New Outlook draft handoff."
readme = "README.md"
requires-python = ">=3.11"
authors = [{ name = "Xinyu Wei" }]
dependencies = [
  "azure-identity==1.25.3",
  "filelock==3.29.7",
  "openai==2.32.0",
  "Pillow==12.3.0",
  "pydantic==2.10.4",
  "python-pptx==1.0.2"
]

[project.scripts]
yunshang = "yunshang.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "SIM"]